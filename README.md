# shopify-component-template

1.) Each component has his own `.scss` file where the style is isolated via the `[role="x"]` selector.

2.) Components can be rendered using `{% render 'x' %}` and their respective props.